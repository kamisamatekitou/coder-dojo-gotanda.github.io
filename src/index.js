import ReactDOM from 'react-dom';
import React from 'react';

const dom = document.getElementById('app');

const Main = (_props) => {
  return <div>codadojo gotanda</div>
};

ReactDOM.render(<Main/>, dom);
